# QR-Generator
Node.js app that prompts for a URL, generates a QR code PNG using qr-image, and saves the URL in a text file. Uses Inquirer for input and fs to write files. Simple, fast QR code creation from any URL via command line.
